# Local browser verification

These are observed working-tree checks from 17 September 2026, not evidence for a final release SHA or hosted preview. All accounts and content are disposable fixtures on the loopback Auth/PostgREST/PostgreSQL stack documented in `scripts/database/README.md`. No production account or student content was used.

## Save-failure harness

`scripts/database/local-fault-proxy.mjs` requires `BINDERNOTES_LOCAL_FAULT_TESTING=true` and listens only on `127.0.0.1:55443`, forwarding only to the real local gateway on port55442. Configure a separate local Vite process to use that URL and the disposable manifest's anon key. The proxy never fabricates a successful response. Auth and reads continue to reach the actual services.

Set `.tmp/remediation/browser-fault.json` to `{"mode":"pass"}`, `{"mode":"reject"}` or `{"mode":"hold"}`. Reject returns an explicit503 for the note, whiteboard and canonical review save RPCs. Hold lets PostgreSQL commit, then buffers the real acknowledgement until pass releases it. This tests uncertain/delayed acknowledgements separately from a rejected write. It is not a browser-wide offline simulation. The file and all browser evidence remain ignored; do not store credentials in reports.

## Observed journeys

| Journey                                               | Observed result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Local evidence under `.tmp/remediation/browser/`                                                                                          |
| ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| Rapid note switching and delayed save acknowledgement | Distinct notes retained their own bodies. A newer edit made while an earlier acknowledgement was held survived navigation, acknowledgement release and reload.                                                                                                                                                                                                                                                                                                                                                                                  | `delayed-ack-reload-newer-note.txt`                                                                                                       |
| Board and route switching                             | A rectangle in board one and ellipse in board two survived immediate switching, leaving the route and returning. After the dark-canvas contrast correction, the original rectangle remained and controls were legible.                                                                                                                                                                                                                                                                                                                          | `board-two-route-return.png`, `board-one-switch-return.png`, `whiteboard-contrast-fixed.png`                                              |
| Account isolation                                     | Signing out learner A and signing in learner B at A's private route showed no A content. A learner opening the operator route returned to the dashboard.                                                                                                                                                                                                                                                                                                                                                                                        | `account-B-private-route-denied.txt`, `learner-admin-redirect.txt`                                                                        |
| Failed save, reload and reconnection                  | Explicit503 showed Save failed while retaining the body. Reload restored the draft. B could not see it. Returning as A after reconnect saved it, and another reload retained the saved body.                                                                                                                                                                                                                                                                                                                                                    | `blocked-save-draft-recovered.txt`, `blocked-draft-account-B-denied.txt`, `reconnected-draft-saved-reload.txt`                            |
| Concurrent edits in independent browser origins       | After one session saved a newer revision, the stale session displayed a conflict and preserved its draft. Saving a separate recovery copy retained the older draft while keeping the cloud winner; reload verified the copy.                                                                                                                                                                                                                                                                                                                    | `two-session-conflict.txt`, `two-session-conflict-copy-reload.txt`                                                                        |
| Quiz results and ownership                            | Created a three-question quiz, submitted actual multiple-choice/numeric/written answers, then reloaded the historical result URL. Saved answers and score2/3 remained. Exact-text written scoring honestly showed Review. Learner B received Saved attempt unavailable at the same result URL.                                                                                                                                                                                                                                                  | `quiz-results-reload.txt`, `quiz-results-other-owner-denied.txt`                                                                          |
| Review scheduling and separate-origin retrieval       | Enabled Review Queue beta, created a cloud card, revealed its answer, rated Good and reloaded. Upcoming retained the card and its20September due date. A separate local browser origin retrieved the source-linked notebook card and its schedule; the archive comparison independently preserved history. This is actual account retrieval, not a claim of live collaborative sync.                                                                                                                                                            | `review-upcoming-reload.txt`, `review-second-origin-source.txt`, `review-second-origin-schedule.txt`, `archive-roundtrip-comparison.json` |
| Course trash and restore                              | Trashing a course hid its document and attached note while leaving an unrelated recovery copy visible. Permanent deletion displayed an explicit DELETE field with disabled confirmation; no permanent deletion was executed. Restore and reload brought back the same course/document relationships and saved body.                                                                                                                                                                                                                             | `trash-explicit-confirmation.txt`, `course-restore-reload.txt`                                                                            |
| Archive roundtrip                                     | Downloaded an archive via the UI and imported it into a newly created empty learner account. Three note/document bodies, one notebook, two review cards and one history event survived. A second UI export compared equal for content, relationships, source references, schedules and history after deliberate ID/owner remapping; server-generated timestamps/revisions were excluded. Malformed version999 was rejected before import; its raw validation error prompted a user-message correction. This fixture contained no binary assets. | `archive-import-complete.txt`, `archive-import-notes-reload.txt`, `archive-roundtrip-comparison.json`, `archive-malformed-rejected.txt`   |
| Creator and operator roles                            | A learner-role account with a real seeded Studio entitlement saved its own binder and lesson, published the binder and reopened it after reload. Its admin-route visit redirected to Workspace. The operator could open Admin studio. This tests existing entitlements, not a Stripe purchase.                                                                                                                                                                                                                                                  | `creator-published-reload.txt`, `creator-admin-denied.txt`, `operator-admin-allowed.txt`                                                  |
| Phone and keyboard interaction                        | At390×844, edited an imported note, activated Save with Enter and reloaded the saved text. Fixed clipped notebook toolbar controls; DOM bounds confirmed all six controls fit within390pixels. Added an accessible name to the icon-only logout button.                                                                                                                                                                                                                                                                                         | `mobile-note-edit-reload.txt`, `mobile-toolbar-fixed.png`                                                                                 |

Additional confirmed checks:

- **Revoked session:** visiting a private route after server-session revocation redirected to `/auth?next=%2Fnotes`; `revoked-session-navigation.txt` records the observed sign-in route. Actual rejected-token API probes and draft-retention/account-race tests are separate evidence; this browser snapshot alone does not prove every retention path.
- **Chemistry checkpoint and layout:** the real local learner-B browser saved a 5.00 mL titration checkpoint at pH 1.18 with measurements, hypothesis and observations, then restored it after hard reload using Reopen snapshot. The first run exposed a missing trusted Chemistry catalog/layout-save failure. After fe0a48c and the local trusted seed, the chosen Acid-Base Titration Lab preset survived hard reload with Ready status, and the original checkpoint/notebook text reopened again. See [the detailed acceptance and screenshots](../artifacts/be15-browser/README.md), committed in 9ac8349. These screenshots distinguish the original failure from the final correction; the separate Smart lab notebook is a different tool.

Observed integration defects were fed back into implementation: invalid paragraph nesting in quiz prompts, inaccessible question fields, empty course states, raw archive validation errors, clipped mobile tools and revoked-session empty-state behavior. Historical screenshots may show the failure before its correction. A later local final-candidate pass is still required.

The development server was changing during these checks. Historical HMR errors are not a clean-console release result. Repeat the complete required journeys against the final built candidate, then against its exact-SHA hosted preview. Real Storage HTTP, cron workers, delivered recovery email, OAuth and Stripe test-mode end-to-end verification remain separate gates.
