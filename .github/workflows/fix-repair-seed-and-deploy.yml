name: Legacy repair and deploy retired
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  migration-safety:
    runs-on: ubuntu-latest
    steps:
      - name: Require reviewed migration history and release evidence
        run: |
          echo "This legacy shortcut is retired. Use Validate BinderNotes and docs/RELEASE_PROCESS.md."
          echo "No hosted credentials are loaded; no history repairs, schema writes, seed changes or deployments run here."
          exit 1
